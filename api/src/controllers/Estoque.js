import Estoque from "../model/Estoque.js"

const EstoqueController = {
    cadastro: async (req,res) => {
        // dar a ideia para o joao de quando cadastrar  produto er opcao de estoque junto
    }
}